---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: traefik
  labels:
    app: traefik
spec:
  podMetricsEndpoints:
    - path: /metrics
      port: traefik
  jobLabel: traefik
  selector:
    matchLabels:
      app: traefik
