---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: dashboard
spec:
  entryPoints:
    - https
  routes:
    - kind: Rule
      match: "Host(`traefik.${BASE_DOMAIN}`)"
      services:
        - name: api@internal
          kind: TraefikService
  # tls:
  #   certResolver: default
